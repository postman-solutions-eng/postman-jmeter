name: JMeter Load Test (repository_dispatch)

on:  
  repository_dispatch:
    types: [load-test-image]

jobs:
  load-test-with-jmeter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install postman to jmeter converter
   #     run: | 
   #       npm install -s postman-jmeter
        run: npm install
      - name: install jmeter
        run: |
          java -version
          wget https://dlcdn.apache.org//jmeter/binaries/apache-jmeter-5.5.zip
          unzip apache-jmeter-5.5.zip
          cd apache-jmeter-5.5/bin
          ./jmeter -v
          pwd
      - name: Convert postman collection
        run: | 
          node ./bin/convert-postman-jmeter -p ${{ github.event.client_payload.collectionPath }} -j ./assets/jmx/${{ github.run_id }}.jmx -o -v=${{ github.event.client_payload.variableStrategy }}
      - name: Run Stress Tests
        run: |
          cd ./apache-jmeter-5.5/bin 
          ./jmeter -n -t ${{ github.workspace }}/assets/jmx/${{ github.run_id }}.jmx -l ${{ github.workspace }}/results.csv -e -o ${{ github.workspace }}/output
      - name: Generate screenshot of JMeter results
        run: |
          npm install --global capture-website-cli
          capture-website output/index.html --output=./assets/images/screenshot_${{ github.run_id }}.png --width 1280 --height 800
      - name: Archive production artifacts
        uses: actions/upload-artifact@v3
        with:
          name: jmeter-load-test-image
          path: screenshot_${{ github.run_id }}.png
      - name:  Add image to repo
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git status
          # mkdir -p assets/images/
          # mv screenshot_${{ github.run_id }}.png assets/images/
          git add assets/images/screenshot_${{ github.run_id }}.png

          git commit -m "added assets/images/screenshot_${{ github.run_id }}.png"
          git push
          git status
          echo "https://github.com/${{github.repository}}/blob/main/assets/images/screenshot_${{ env.GITHUB_RUN_ID }}.png?raw=true"
      - name: Send results to postman
        run: | 
          node ./bin/respond -w  ${{ github.event.client_payload.workspaceID }} -u https://github.com/postman-solutions-eng/postman-jmeter/blob/main/assets/images/screenshot_${{ github.run_id }}.png -r $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID -k ${{ secrets.POSTMAN_API_KEY }} -n $GITHUB_RUN_NUMBER

